{
  "name": "neotool",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["packages/*","plugins/*","presets/*"],
  "scripts": { "build": "pnpm -r run build", "dev": "pnpm -r run dev", "lint": "eslint . --ext .ts,.tsx", "test": "vitest run" },
  "devDependencies": { "typescript": "^5.4.0", "eslint": "^9.0.0", "prettier": "^3.2.0", "vitest": "^1.3.0" }
}
